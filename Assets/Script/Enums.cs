using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enums
{
    public enum GameStatus { None, Run, Pause, GameOver }
    public enum Team { None, Player, Enemy }

    public enum SpawnType { Enemy, Projectile }
}
